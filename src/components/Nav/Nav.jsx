import { NavLink } from 'react-router-dom';
import styles from './Nav.module.css'

const Nav = () => {
  return (
    <div className={styles.Nav}>
      <NavLink></NavLink>
    </div>
  )
};

export default Nav